# MiHotKey

Небольшая Windows-утилита (tray) для привязки триггеров (глобальные хоткеи, WMI-события) к действиям над целевым окном: отправка сочетаний клавиш или запуск программы. Также поддерживаются кастомные клавиши на клавиатуре <b>Xiaomi Mi Gaming Laptop</b>.

## Быстрый старт

- Сборка: `dotnet build MiHotKey.sln -c Release`
- Запуск из исходников: `dotnet run --project MiHotKeyApp`
- Конфиг: `MiHotKeyApp/config.json` копируется в выходную папку и читается приложением при старте.

## Конфигурация

Схема и примеры — в `CONFIG.md`.
